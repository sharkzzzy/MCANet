

好的，我现在对照你的全文原文，逐一给出精确的修改方案。

---

# 全文重复精简——逐条修改方案

---

## 一、SSM原理重复讲3遍（最高优先级，省1-1.5页）

### 位置1：1.1节绪论（第4段）

**当前原文：**

> 近年来，状态空间模型（State Space Model, SSM）[13]凭借其线性计算复杂度和全局序列建模能力，为解决这一矛盾提供了新的技术路径。以Mamba[14]为代表的选择性状态空间模型通过输入依赖的选择机制，在保持线性复杂度的同时实现了对长序列的高效建模，已在自然语言处理和计算机视觉等领域展现出巨大潜力。在遥感图像分割中，基于Mamba的方法[15]已初步验证了其在处理大规模场景时兼顾精度与效率的可行性。然而，现有方法仍存在全局上下文建模与局部细节增强在单一处理路径中相互耦合、注意力机制在网络不同阶段冗余堆叠等问题，限制了模型性能的进一步提升。

**改为：**

> 近年来，状态空间模型（State Space Model, SSM）[13]凭借其线性计算复杂度和全局序列建模能力，为解决这一矛盾提供了新的技术路径。以Mamba[14]为代表的选择性状态空间模型在保持线性复杂度的同时实现了对长序列的高效建模，在遥感图像分割中已初步验证了兼顾精度与效率的可行性[15]。然而，现有方法仍存在全局上下文建模与局部细节增强在单一处理路径中相互耦合、注意力机制在网络不同阶段职能重叠等问题，限制了模型性能的进一步提升。

**说明：** 压缩了1句（删掉"已在自然语言处理和计算机视觉等领域展现出巨大潜力"），技术细节留给1.2.3节。同时"冗余堆叠"改为"职能重叠"，与全文统一措辞。

---

### 位置2：1.2.3节国内外研究现状"基于状态空间模型的语义分割方法"——保留不动

这一节是SSM原理的**唯一完整介绍位置**，从连续方程→离散化→S4→Mamba→SS2D→VMamba→遥感应用，全部保留。

---

### 位置3：第二章2.3节"状态空间模型"——精简

这是重复最严重的位置。2.3节整节（2.3.1连续状态空间模型 + 2.3.2离散化与计算实现 + 2.3.3从S4到Mamba的演进 + 2.3.4 SS2D）与1.2.3节高度重复。

**但考虑到第二章定位是"理论基础"，应保留公式推导和数学细节，而1.2.3节侧重发展脉络综述。两者的区分是：1.2.3节讲"做了什么、为什么重要"，2.3节讲"具体怎么算"。**

因此2.3节的修改策略是：**删掉与1.2.3节重复的定性叙述和发展故事，只保留公式推导和数学解释。**

#### 2.3节开头段

**当前原文：**

> 状态空间模型是本文网络架构的核心理论基础，DP-UNet解码器中的DVSS模块即以状态空间模型作为全局上下文建模的核心引擎。本节将从连续状态空间模型的基本定义出发，阐述其在深度学习中的离散化实现方式，进而分析从S4到Mamba的关键技术突破，最后介绍将一维SSM扩展到二维图像处理的SS2D机制。

**改为：**

> 状态空间模型是本文网络架构的核心理论基础。1.2.3节已从技术演进的角度综述了SSM从S4到Mamba再到视觉领域应用的发展历程，本节将聚焦于其数学原理与计算实现，为后续第三章的方法设计提供严格的公式基础。

---

#### 2.3.1 连续状态空间模型

**当前第一段原文：**

> 状态空间模型起源于控制理论和信号处理领域，是一类通过隐状态的线性动力学方程来描述系统输入输出关系的数学模型。其基本思想是：系统在任意时刻的行为可以通过一组"隐状态"来完整描述，这些隐状态在外部输入的驱动下随时间演化，而系统的输出则由当前隐状态决定。

**改为：**

> 本节给出连续时间状态空间模型的数学定义。SSM通过隐状态的线性动力学方程描述输入到输出的映射关系，其连续形式由以下两个方程定义：

**说明：** 删掉"起源于控制理论"的背景介绍（1.2.3节已讲过），直接进入公式。

其余公式和公式解释（状态方程、观测方程、A/B/C/D矩阵的含义）**保留不动**，这些是数学定义，不算重复。

**最后一段原文：**

> 状态空间模型与循环神经网络（RNN）在形式上具有相似性——两者都通过隐状态的递推来积累序列信息。然而，SSM的关键优势在于其线性动力学特性：状态方程中不包含非线性激活函数，隐状态的演化完全由线性运算驱动。这一特性使得SSM可以被转化为全局卷积的形式进行高效并行计算（详见2.3.2节），克服了RNN只能逐步递推、无法并行训练的根本缺陷。

**保留不动。** 这段讲的是SSM的数学性质（线性→可卷积化），属于理论层面的解释，1.2.3节没有这个角度，不算重复。

---

#### 2.3.2 离散化与计算实现

**整小节保留不动。** 这节讲ZOH离散化公式推导、递推模式与卷积模式的等价性，全是数学内容，1.2.3节只是一句话带过，不重复。

---

#### 2.3.3 从S4到Mamba的演进

这一小节与1.2.3节重复最严重。1.2.3节已经完整讲了S4的HiPPO初始化、LTI局限、Mamba的选择性参数化、硬件感知并行扫描。

**修改策略：删掉S4和Mamba的发展故事和动机叙述，只保留Mamba选择性参数化的公式定义（因为第三章DVSS需要引用这些公式）。**

**当前原文（很长，约1.5页）** 从"理解了SSM的基本原理后"开始，到Mamba的并行扫描算法结束。

**替换为以下精简版本：**

> **2.3.3 选择性状态空间模型**
>
> S4通过HiPPO初始化和结构化参数设计首次实现了SSM在长序列建模中的突破，但其参数为输入无关的固定值（线性时不变），无法根据输入内容自适应地调整信息处理策略。Mamba将这一限制从根本上突破，其关键改进已在1.2.3节详细论述，此处给出本文方法设计所依赖的核心公式定义。
>
> Mamba将参数$\mathbf{B}$、$\mathbf{C}$和步长$\Delta$参数化为输入$x$的函数：
>
> $$\mathbf{B}_t = s_B(x_t), \quad \mathbf{C}_t = s_C(x_t), \quad \Delta_t = \text{softplus}(s_\Delta(x_t))$$
>
> 其中$s_B$、$s_C$和$s_\Delta$为可学习的线性投影。输入依赖的$\Delta_t$控制隐状态的更新幅度：较大的$\Delta_t$使当前输入被强力写入隐状态，较小的$\Delta_t$则保持先前记忆几乎不变。$\mathbf{B}_t$和$\mathbf{C}_t$的输入依赖性分别使模型能够选择性地编码和读取隐状态信息。由于参数在每个时间步均不同，S4中基于固定卷积核的并行计算不再适用，Mamba通过硬件感知的并行扫描算法将递推计算重构为并行前缀和运算，保持了$O(L)$的理论复杂度。

**说明：** 从约1.5页压缩到约半页。删掉了：S4面临的挑战细节、HiPPO详细解释、DPLR参数化、LTI特性的详细分析、Mamba三种选择性的逐条分析、[此处插入图]的S4与Mamba对比图（该图可以放在1.2.3节）。保留了公式和核心物理解释。

---

#### 2.3.4 二维选择性扫描机制（SS2D）

这一小节与1.2.3节的SS2D介绍也有重复。

**当前原文** 从"Mamba最初为一维序列数据设计"开始，讲了raster scan的问题、四方向扫描的设计、SS2D的完整处理流程。

**修改策略：删掉动机叙述（1.2.3节已讲），保留四方向扫描的简要说明和SS2D的完整处理流程（因为第三章DVSS直接使用SS2D，需要知道输入输出格式）。**

**当前第一、二段（约半页的动机叙述）：**

> Mamba最初为一维序列数据（如文本、语音）设计，将其应用于二维图像数据面临一个根本性挑战......如果简单地采用逐行扫描（raster scan）将二维图像展开为一维序列，会产生一个严重问题......更严重的是，单一方向的扫描引入了方向性偏差......

**替换为：**

> 将一维SSM扩展到二维图像的核心挑战及其解决思路已在1.2.3节讨论，本节给出SS2D模块的具体计算流程，为第三章DVSS模块的设计提供技术基础。

四个扫描方向的列表**保留不动**（方向1-4），这是具体的实现细节。

[此处插入图：SS2D四方向扫描示意图] **保留不动。**

**当前"通过这种四方向交叉扫描策略"段落** 讲任意两点信息可达——**删掉**，1.2.3节已有同样论述。

**当前最后一段"SS2D模块的完整处理流程"** 从"输入特征$X$首先通过线性投影"到"远低于Transformer自注意力的复杂度"——**保留不动**，这是第三章需要引用的技术细节。

**当前最后一句：**

> 本文在DP-UNet的DVSS模块中采用SS2D作为共享基座，利用其高效的全局建模能力为后续的全局路径和局部路径提供共同的基础特征表示。

**保留不动。** 这是连接第三章的过渡句。

---

## 二、研究动机/问题反复重述（高优先级，省0.5-1页）

### 位置1：1.2.3节末尾的问题分析——保留不动

**当前原文（"尽管上述方法取得了显著进展"到该小节结束）** 这是首次完整提出两个问题，保留。

### 位置2：1.3节主要研究内容开头

**当前原文：**

> 针对上述研究现状中存在的问题，本文围绕基于状态空间模型的遥感图像语义分割方法展开研究，致力于解决现有方法中全局与局部特征建模耦合、注意力机制冗余以及多模态特征融合不充分等问题。具体研究内容如下：

**改为：**

> 针对上述问题，本文围绕基于状态空间模型的遥感图像语义分割方法展开研究。具体研究内容如下：

**说明：** 删掉"致力于解决现有方法中全局与局部特征建模耦合、注意力机制冗余以及多模态特征融合不充分等问题"——这句话与1.2.3节末尾和1.2.4节末尾完全重复。"针对上述问题"四个字已经足够引用。

### 位置3：1.3节（1）段开头

**当前原文：**

> （1）针对现有基于SSM的遥感分割方法中全局上下文建模与局部细节增强在单一路径中耦合处理、注意力机制在不同网络阶段冗余堆叠等问题，本文提出了基于双路径解耦的语义分割网络DP-UNet。全局上下文建模与局部细节增强是语义分割中两个本质不同的子任务：前者需要在较大空间范围内整合语义信息以保证类别预测的区域一致性，后者则需要在精细空间尺度上保留高频细节以确保分割边界的准确性。现有方法将两者置于同一处理路径中顺序执行，导致两种任务的参数优化相互制约。同时，功能相似的注意力机制在特征变换和特征融合等多个阶段被重复施加，带来了不必要的计算冗余。

**改为：**

> （1）针对1.2.3节所分析的全局-局部耦合与注意力职能重叠问题，本文提出了基于双路径解耦的语义分割网络DP-UNet。

**说明：** 删掉了约100字的问题重述。这些内容在1.2.3节末尾已经完整表述过，此处只需一句话引用即可，然后直接进入解决方案的描述（"为此，本文在解码器中设计了双路径解耦VSS模块……"）。

### 位置4：第三章开头段

**当前原文（第三章第一段）：**

> 遥感图像语义分割是遥感图像智能解译中的核心任务，其目标是将图像中的每个像素分配至对应的地物类别，从而实现对地表覆盖信息的精确提取与定量分析。这一技术在城市规划、环境监测、灾害评估等领域具有广泛的应用价值。高分辨率遥感图像具有地物类型丰富、目标尺度差异显著、背景环境复杂等特点，这对分割模型同时提出了全局上下文建模能力与局部细节捕获能力的双重要求。然而，现有基于状态空间模型的分割方法普遍采用单路径的序列化处理架构，将上述两种本质不同的子任务耦合于同一计算流程中，导致优化过程中出现目标冲突。此外，在多阶段特征处理中，通道-空间注意力的同质化重复施加也引入了不必要的计算冗余。

**改为：**

> 如第一章所分析，现有基于状态空间模型的遥感图像分割方法存在全局-局部建模耦合与注意力职能重叠两个共性问题。本章针对这两个问题，提出了一种基于双路径解耦思想的遥感图像语义分割网络DP-UNet，核心设计遵循"解耦"与"精简"两大原则。在解码器中设计了双路径解耦状态空间模块（Dual-path VSS, DVSS），将全局上下文建模与局部细节增强分离为两条独立的专用路径；在多尺度特征融合阶段，提出了轻量级的多尺度空间核模块（Multi-Scale Kernel, MSK），以纯空间注意力取代重复施加的通道-空间双重注意力，在保持融合效果的同时显著降低了计算开销。

**说明：** 把原来的两段合并为一段。删掉了第一段的背景重述（"遥感图像语义分割是核心任务……"在1.1节已讲过）和问题重述（"现有方法普遍采用单路径……"在1.2.3节已讲过），用"如第一章所分析"一句话引用。第二段的方法概述保留但前置。

### 位置5：3.1节"现有方法的问题分析"

这一节（3.1.1全局-局部建模的耦合瓶颈 + 3.1.2注意力机制的冗余施加）与1.2.3节末尾的问题分析**高度重复**。

**但这一节有1.2.3节没有的内容：图3-1失败案例、图3-2注意力权重可视化。这些是第三章独有的，应保留。**

**修改策略：每小节的前半段动机叙述大幅精简（引用1.2.3节），后半段的图示分析和设计策略保留。**

#### 3.1.1 全局-局部建模的耦合瓶颈

**当前第一段：**

> 遥感图像语义分割本质上需要模型同时完成两个层面的推理任务：一是全局上下文建模，即在较大空间范围内整合语义信息，识别地物类别的宏观分布模式，确保同类地物在不同空间位置的分类一致性；二是局部细节增强，即在精细空间尺度上保留并强化边缘轮廓、纹理特征等高频信息，确保地物边界的准确描绘和小目标的有效识别。这两个任务虽然作用于同一输入特征，但其优化目标存在内在差异——前者追求特征的平滑性和语义一致性，后者追求特征的锐利性和空间精确性。

**改为：**

> 如1.2.3节所分析，全局上下文建模与局部细节增强具有本质不同的优化目标——前者追求特征的平滑性和语义一致性，后者追求特征的锐利性和空间精确性。现有方法将两者耦合于同一路径中，导致参数优化相互制约。

**当前第二段（"以基于状态空间模型的分割网络为例……"）：** 保留，因为它以CM-UNet的CSMamba为具体案例分析耦合问题，比1.2.3节更具体。

**当前第三段（"为了更直观地理解……图3-1"）：** 保留不动。

**当前第四段（"上述分析表明……"）：** 保留不动。

#### 3.1.2 注意力机制的冗余施加

**当前第一段：**

> 注意力机制已成为现代深度网络中增强特征表达能力的标准手段。在遥感分割任务中，通道注意力和空间注意力分别从特征通道间依赖建模和空间位置间关联建模两个维度提升特征的判别力，被广泛应用于网络的各个阶段。然而，当注意力模块在网络中被不加区分地重复施加时，其边际收益将迅速递减，而带来的参数量和计算量增长却是线性累积的。

**改为：**

> 如1.2.3节所指出，现有方法中注意力机制存在跨阶段的职能重叠问题。本节以CM-UNet为具体案例，分析这一问题的两个层面。

**第二段（"具体而言，现有方法中注意力冗余主要体现在两个层面……"）起：** 保留不动。这段是以CM-UNet的CSMamba和MSAA为具体案例的分析，比1.2.3节更详细。

**最后一段（"基于上述分析，本章主张……"）：** 保留不动，这是设计策略的总结。

---

## 三、贡献点反复列举（高优先级，省0.5页）

本文没有独立的"1.X节 主要贡献"章节（贡献点融合在1.3节的研究内容中），所以这个问题不如预想的严重。但以下位置存在重复：

### 位置1：第三章本章小结

**当前原文（第二段，"具体而言"起）：**

> 具体而言，本章首先通过系统的问题分析，揭示了现有基于状态空间模型的分割方法中存在的两个共性问题：全局-局部建模的耦合效应导致两个子任务无法各自达到最优，以及多阶段同质注意力的重复施加带来的计算冗余与注意力过度聚焦现象。基于上述分析，本章提出了双路径解耦状态空间模块（DVSS），将全局上下文保持和局部细节增强分离至两条独立路径中。全局路径直接保留状态空间扫描的全局语义表示，局部路径则通过ECA通道注意力进行特征筛选后，利用PMC参数域调制卷积抑制标准卷积的中心主导效应，增强对边缘和纹理等局部细节的捕获能力。两条路径的输出通过APFG自适应路径融合门控进行加权整合。在多尺度特征融合阶段，本章提出了轻量级的MSK模块，以纯空间注意力取代冗余的通道-空间双重注意力，在压缩通道空间中完成多尺度空间模式提取与空间注意力调制，实现了注意力参数80%以上的压缩。

**改为：**

> 具体而言，本章提出了DVSS模块实现全局-局部双路径解耦处理，以及MSK模块实现精简化的多尺度空间融合，两者协同构成了DP-UNet的核心设计。

**说明：** 本章小结应侧重**实验结论**而非重复方法描述。方法已在3.3和3.4节详细介绍过，小结中无需再复述。后续的实验结论段落保留不动。

### 位置2：第五章总结与展望

第五章5.1节的贡献总结确实很长（占了约2页），但作为毕业论文的最终总结章，这里的详细回顾是**合理且必要的**——评审老师可能只看绪论和总结。所以**第五章保留不动**。

---

## 四、VSS Block / SS2D结构重复描述（中优先级，省0.3页）

### 位置1：1.2.3节

**当前原文中关于VMamba/SS2D的介绍：**

> 针对这一挑战，Liu等人[35]提出了VMamba，设计了二维选择性扫描模块（SS2D）。SS2D的核心思想是沿四个方向……分别将二维特征图展开为一维序列，对每个方向独立进行选择性SSM扫描，最后将四个方向的输出进行融合。这种交叉扫描策略确保了特征图上任意两个位置之间至少存在一条扫描路径使其信息可达，从而在保持线性复杂度的同时实现了对二维空间全局依赖关系的有效建模。

**保留不动。** 这是综述性描述，讲"做了什么"。

### 位置2：2.3.4节

在上面"一、SSM原理重复"中已经处理过——删掉了动机叙述，保留了四方向列表和SS2D完整处理流程。

### 位置3：3.3.1节"共享基底与路径分离"

**当前原文：**

> ……首先经过层归一化处理，然后输入SS2D模块。SS2D模块采用四方向二维状态空间扫描机制，将二维特征图沿四个方向（从左到右、从右到左、从上到下、从下到上）展开为一维序列，分别进行状态空间建模后合并，从而在保持线性计算复杂度的同时捕获全局范围的特征依赖关系。

**改为：**

> ……首先经过层归一化处理，然后输入SS2D模块（其原理与计算流程详见2.3.4节）进行全局范围的状态空间扫描。

**说明：** SS2D的工作原理在2.3.4节已有完整介绍，此处一句话引用即可，直接进入本章的创新点——"与现有方法不同的是，本章对SS2D模块进行了精简化处理"。

---

## 五、多模态融合分类重复（中优先级，省0.2页）

### 位置1：1.2.4节"多模态遥感图像融合分割方法"

**当前第二段：**

> 根据融合阶段的不同，多模态融合方法通常可分为早期融合、中期融合和晚期融合三种策略[42]。早期融合在输入层直接拼接不同模态的数据……晚期融合在决策层……中期融合在特征提取的中间阶段……是目前研究最为广泛且效果最为显著的融合策略。

**保留不动。** 这是首次介绍。

### 位置2：4.1.2节"现有融合策略的局限性"

**当前原文：**

> 从融合策略的角度审视，现有多模态遥感图像分割方法可大致归纳为三类范式，各自存在不同程度的局限性。
> 第一类是早期融合策略，即在网络输入层将不同模态的图像沿通道维度直接拼接后，使用单一编码器进行特征提取。这种策略的优势在于结构简洁、实现方便，但其本质假设是共享的卷积核能够同时适配两种差异巨大的模态特征，这在实际中难以成立。由于光学特征和SAR特征的统计分布存在根本性差异，共享卷积核难以在两种模态上同时学习到最优的特征表示，导致特征提取的质量受限。
> 第二类是晚期融合策略……
> 第三类是中间融合策略……

**改为：**

> 如1.2.4节所述，现有多模态融合策略可分为早期融合、中期融合和晚期融合三类。在光学-SAR融合的具体场景下，这三类策略各自面临针对性的局限：早期融合的共享卷积核难以同时适配光学图像平滑的光谱反射特征和SAR图像粗糙的散射纹理特征；晚期融合使两种模态在整个编码过程中完全独立演化，错失了中间层级建立跨模态交互的机会；中期融合虽然在理论上最为充分，但面临多层级交互计算开销大以及统一融合策略难以适应各层级特征特性的挑战。

**说明：** 从三段压缩为一段。保留了三类策略的核心局限分析（特别是补充了光学-SAR场景的具体化分析），删掉了通用性的定义（1.2.4节已讲）。

**当前"此外"段（关于多尺度问题的分析）：** 保留不动，这是4.1节独有的论点。

---

## 六、数据集描述重复（低优先级，省0.1-0.2页）

### 位置1：2.5节数据集介绍——保留不动

第二章的数据集介绍包含完整的来源、特点、挑战分析和示例图，是**唯一的完整介绍位置**。

### 位置2：3.4.1节"数据集介绍"

**当前原文** 对Potsdam、Vaihingen、LoveDA三个数据集各有一段介绍，与2.5节有部分重复。

**修改策略：保留实验相关的关键信息（训练/测试划分），删掉数据集的通用性描述。**

#### Potsdam

**当前原文：**

> ISPRS Potsdam数据集由国际摄影测量与遥感学会发布，包含波茨坦市的高分辨率航空正射影像，地面采样距离为5厘米。数据集提供近红外、红色和绿色（NIRRG）三通道影像及对应的数字表面模型。地物标注涵盖六个类别：不透水面（Impervious surfaces）、建筑物（Building）、低矮植被（Low vegetation）、树木（Tree）、车辆（Car）和杂项（Clutter）。本章实验中，选取23幅影像用于训练（排除存在标注错误的7_10号影像），14幅影像用于测试。

**改为：**

> ISPRS Potsdam数据集的基本信息与数据特点已在2.5.1节详细介绍。本章实验中，选取23幅影像用于训练（排除存在标注错误的7\_10号影像），14幅影像用于测试，训练和测试时将原始图像裁剪为512×512像素的图像块。

#### Vaihingen

**当前原文：**

> ISPRS Vaihingen数据集同样由国际摄影测量与遥感学会发布，包含法伊欣根市的航空正射影像，地面采样距离为9厘米。影像为NIRRG三通道格式，地物类别标注与Potsdam数据集一致，包含六个类别。本章实验中选取12个图像块用于训练，4个图像块用于测试。Vaihingen数据集相较于Potsdam数据集，影像面积较小但地物分布更为复杂，包含更多的阴影区域和不规则建筑形态，对模型的细节捕获能力提出了更高的要求。

**改为：**

> ISPRS Vaihingen数据集的基本信息与数据特点已在2.5.2节详细介绍。本章实验中选取12个图像块用于训练，4个图像块用于测试。

#### LoveDA

**当前原文：**

> LoveDA数据集包含来自中国南京、常州和武汉三个城市的高分辨率遥感影像，地面采样距离为0.3米。该数据集涵盖城市和农村两种典型场景，标注类别为七类：背景（Background）、建筑物（Building）、道路（Road）、水体（Water）、裸地（Barren）、森林（Forest）和农田（Agriculture）。训练集包含1156幅影像，验证集包含677幅影像。LoveDA数据集的特点在于城乡场景差异显著、类别分布严重不均衡，且包含大面积的混合过渡区域，是评估模型在复杂真实场景下泛化能力的有力基准。

**改为：**

> LoveDA数据集的基本信息与数据特点已在2.5.3节详细介绍。本章实验中，训练集包含1156幅影像，验证集包含677幅影像，按照官方划分进行训练和测试。

### 位置3：4.4.1节WHU-OPT-SAR数据集

**当前原文：**

> 本章实验在WHU-OPT-SAR数据集上进行评估。该数据集由武汉大学发布，包含同一地理区域的配准光学图像和SAR图像，覆盖了武汉市及其周边地区的城市和乡村场景。数据集的光学图像为四通道（RGB+NIR）格式，SAR图像为单通道格式。地物标注涵盖七个语义类别：耕地（Farmland）、城市（City）、村庄（Village）、水体（Water）、森林（Forest）、裸地（Road）和其他（Others）。训练中对未标注区域（标签值255）设置忽略，不参与损失计算。

> WHU-OPT-SAR数据集的特点在于……

**改为：**

> 本章实验在WHU-OPT-SAR数据集上进行评估，该数据集的基本信息与数据特点已在2.5.4节详细介绍。本章实验中，训练中对未标注区域（标签值255）设置忽略，不参与损失计算。所有图像裁剪为256×256像素的图像块，按照标准划分进行训练和测试。

**说明：** 删掉了数据集的通用性介绍（来源、覆盖范围、通道数、类别列表、特点分析），这些在2.5.4节已完整覆盖。只保留本章实验特有的设置（忽略标签255、裁剪尺寸）。

---

## 汇总检查单

| 修改位置 | 修改类型 | 预估节省 |
|----------|---------|---------|
| 1.1节第4段 | 压缩SSM介绍，统一措辞 | 1行 |
| 1.3节开头 | 删掉问题重述 | 2行 |
| 1.3节（1）段开头 | 删掉问题重述 | 5行 |
| 2.3节开头段 | 引用1.2.3节 | 1行 |
| 2.3.1节第一段 | 删掉背景叙述 | 3行 |
| 2.3.3节 | **大幅精简**，从1.5页→0.5页 | **约1页** |
| 2.3.4节前两段 | 删掉动机叙述 | 0.3页 |
| 第三章开头两段 | 合并精简 | 0.3页 |
| 3.1.1节第一段 | 引用1.2.3节 | 4行 |
| 3.1.2节第一段 | 引用1.2.3节 | 3行 |
| 3.3.1节SS2D介绍 | 引用2.3.4节 | 2行 |
| 3.6节本章小结第二段 | 大幅精简方法复述 | 0.3页 |
| 4.1.2节融合三分类 | 压缩为一段 | 0.3页 |
| 3.4.1节三个数据集描述 | 各精简为1-2句 | 0.3页 |
| 4.4.1节数据集描述 | 精简为2句 | 0.2页 |
| **合计** | | **约3页** |

所有修改均可直接执行，不影响论文的完整性和逻辑链条。
